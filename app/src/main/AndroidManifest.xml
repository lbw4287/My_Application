<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".FoodDetail_tri"></activity>
        <activity android:name=".FoodDetail_X" />
        <activity android:name=".FoodDetail" />
        <activity android:name=".NutsList" />
        <activity android:name=".FishList" />
        <activity android:name=".MeatList" />
        <activity android:name=".VegList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FruitList" />
        <activity android:name=".SearchAdapter" />
        <activity android:name=".row_listview" />
    </application>

</manifest>